apiVersion: v1
kind: Service
metadata:
  name: {{ include "m-time-service.fullname" . }}
  labels:
    {{- include "m-time-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "m-time-service.selectorLabels" . | nindent 4 }}