apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "m-time-service.fullname" . }}-config
  labels:
    {{- include "m-time-service.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}